import React from 'react';
import { Loader } from '@mantine/core';

const code = `
import { Loader } from '@mantine/core';

function Demo() {
  return <Loader />;
}
`;

function Demo() {
  return <Loader />;
}

export const usage = {
  type: 'demo',
  component: Demo,
  code,
};
