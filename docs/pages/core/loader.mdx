import { Layout } from '@/layout';

export const meta = {
  title: 'Loader',
  package: '@mantine/core',
  group: 'mantine-core',
  slug: '/core/loader/',
  order: 1,
  category: 'feedback',
  description: 'Indicate loading state',
  props: ['Loader'],
  styles: ['Loader'],
  import: "import { Loader, Notification } from '@mantine/core';",
  source: 'mantine-core/src/components/Loader/Loader.tsx',
  docs: 'core/Loader.mdx',
};

export default Layout(meta);

## Usage

Hello there. Home [page](/).

This is external [link](https://google.com)

```tsx
import { Loader } from '@mantine/core';

function Demo() {
  return <Loader />;
}
```

<Polymorphic component="Loader" defaultElement="div" changeToElement="button" withNext />
